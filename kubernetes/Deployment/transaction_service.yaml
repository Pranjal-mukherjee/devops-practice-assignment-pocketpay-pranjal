apiVersion: apps/v1
kind: Deployment
metadata:
  name: bc117-transaction-service-deployment
  labels:
    type: service
    name: transaction
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: transaction-service-label
    spec:
      containers:
      - name: transaction-service-container
        image: ghcr.io/pranjal-mukherjee/bootcamp117-backend-transaction-service:v8
        ports:
        - containerPort: 9003
        env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://service-registry-svc:8761/eureka/
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://3.14.69.221:30596/pocket_pay
            - name: EUREKA_INSTANCE_HOSTNAME
              value: transaction-service-svc
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                  secretKeyRef:
                      name:  database-secret
                      key:  username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                  secretKeyRef:
                      name:  database-secret
                      key:  password
      imagePullSecrets: 
      - name: ghcr-secret
  selector:
    matchLabels:
      app: transaction-service-label
